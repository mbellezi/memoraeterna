import {describe,it,expect} from 'vitest';
import {renderToStaticMarkup} from 'react-dom/server';
import {createTranslator,supportedLanguageCodes} from '@app/i18n';
import {InterpretationRecordSchema} from '@app/domain';
import {KnowledgeInterpretations} from './KnowledgeInterpretations';
const id='00000000-0000-4000-8000-000000000001';
const record=InterpretationRecordSchema.parse({version:'knowledge-interpretation-v1',assertionId:id,revisionId:id,statement:'Supplied unchanged experience.',perspective:'ai_interpretation',context:'uncertain',eventTime:{precision:'year',value:'2024'},validFrom:{precision:'month',value:'2025-03'},validUntil:null,publicationTime:null,relationships:[],procedure:{steps:['Three sessions'],workedWhen:'One diary observation',assumptions:['Self report'],limits:['Retention unmeasured'],corrections:[]},recordedAt:[{documentId:id,at:'2026-09-12T10:00:00.000Z'}],revisedAt:'2026-09-12T11:00:00.000Z',sourceDates:[{sourceItemId:id,noteDate:'2026-09-01',publicationDate:null}],evidence:[{sourceItemId:id,documentId:id,chunkId:id,sourceSpanId:null,contentHash:'same',excerpt:'I felt less tired; I did not measure retention.',sourceTitle:'Supplied diary',documentCreatedAt:'2026-09-12T10:00:00.000Z',locator:null}]});
describe('native interpretation details',()=>{for(const locale of supportedLanguageCodes)it('keeps precision, author date and qualified experience in '+locale,()=>{const t=createTranslator(locale),html=renderToStaticMarkup(<KnowledgeInterpretations items={[record]} t={t}/>);expect(html).toContain('2024');expect(html).toContain(t('knowledgeEvolution.noteDate'));expect(html).toContain(t('knowledgeEvolution.recordedAt'));expect(html).toContain(t('knowledgeEvolution.ai_interpretation'));expect(html).toContain('Retention unmeasured');expect(html).not.toContain('[[missing:');expect(html).not.toContain('T10:00:00.000Z');expect(html).not.toContain('<h5 class="font-medium">'+t('knowledgeEvolution.corrections'));});});
